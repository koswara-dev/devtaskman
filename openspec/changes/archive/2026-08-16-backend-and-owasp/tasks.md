## 1. Directory Restructuring

- [x] 1.1 Create the `frontend` folder and move all React/Vite folders (`src`, `public`, `index.html`, package JSON and TS configs) into it.
- [x] 1.2 Setup the `backend` directory structure with packages initialization.

## 2. ExpressJS Backend Implementation

- [x] 2.1 Write `backend/package.json` with dependencies (`express`, `cors`, `helmet`, `express-rate-limit`, `express-validator`, `dotenv`) and devDependencies (`typescript`, `ts-node`, `@types/express`, `@types/cors`, `@types/node`). Setup `backend/tsconfig.json`.
- [x] 2.2 Implement backend server in `backend/src/index.ts` pre-seeded with mockup datasets and core CRUD endpoints.
- [x] 2.3 Add custom role validation authorization middleware (enforcing A01:2021) and schema validations (enforcing A03:2021).


